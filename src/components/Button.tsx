
const Button = ({styles}) => {
    return (
    <button type="button" className={`py-4 font-poppins font-medium text-[18px] rounded-[10px] text-primary outline-none px-6 bg-blue-gradient ${styles}`}>
        Get Started
    </button>
)}

export default Button;